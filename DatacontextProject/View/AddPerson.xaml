<Window x:Class="DatacontextProject.View.AddPerson"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DatacontextProject.View"
        mc:Ignorable="d"
        Title="AddPerson" Height="300" Width="300"
        DataContext="{Binding AddPersonView, Source={StaticResource Locator}}">
    <StackPanel>
        <TextBlock>Name</TextBlock>
        <TextBox Text="{Binding person.name}"/>
        <Button Command="{Binding addNewPersonCommand}">Add new person</Button>
    </StackPanel>
</Window>
